import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'machine-shop-mobile-app',
  webDir: 'www'
};

export default config;
